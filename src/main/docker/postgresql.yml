version: '2'
services:
  traderhelperapplication-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/traderhelperApplication/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=traderhelperApplication
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
